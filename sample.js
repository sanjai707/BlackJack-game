let objects = {
    title: "Series i watched",
    rateings: "4.2 above",
    available: true,
    lists: ["game of thrones", "Strangers things", "Vikings", "house of dragons"]
};

console.log(objects.title);
console.log(objects.rateings);
console.log(objects.available);
console.log(objects.lists);

let data = {
    name: "sanjai",
    age: 20,
    country: "India"  // Fixed the typo here
};

console.log(data.name + " is " + data.age + " years old and lives in " + data.country);
